import { PlanDayScreen } from "@/features/dayplan/PlanDayScreen";

/** /planera-dagen — "Planera din dag" (patch 12), the mobile port of the
 * web's /dag DaySetupPlanner. Guarded route: registered inside the auth
 * gate's signed-in block in app/_layout.tsx. */
export default PlanDayScreen;
