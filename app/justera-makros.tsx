import { MacroAdjustScreen } from "@/features/profile/MacroAdjustScreen";

/** /justera-makros — the web's /profil/justera-makros (flat path on mobile;
 * the profile lives in the konto tab rather than at /profil). */
export default MacroAdjustScreen;
