import { LoginScreen } from "@/features/auth/LoginScreen";

/** /logga-in — same path as the web's (auth)/logga-in route. Accepts a
 * ?next= param for the return-to-checkout flow, like the web. */
export default LoginScreen;
