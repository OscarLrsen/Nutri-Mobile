import { CartScreen } from "@/features/cart/CartScreen";

/** /varukorg tab — the cart (Feature 4), ported from the web's
 * src/app/varukorg/page.tsx item list + summary. Checkout, payment and
 * order placement arrive with Feature 7. */
export default CartScreen;
