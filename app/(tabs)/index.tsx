import { HomeScreen } from "@/features/home/HomeScreen";

/** Hem — landing tab. Implementation lives in features/home/ per the
 * feature-module convention (features/README.md). */
export default HomeScreen;
