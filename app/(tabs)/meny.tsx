import { MenuScreen } from "@/features/menu/MenuScreen";

/** Meny-tab. Implementation lives in features/menu/ per the feature-module
 * convention (features/README.md). The infrastructure-phase health-check
 * placeholder that used to live here was retired when the real menu landed —
 * connectivity problems now surface through the menu's own error state. */
export default MenuScreen;
