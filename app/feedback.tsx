import { FeedbackFormScreen } from "@/features/feedback/FeedbackFormScreen";

/** Send feedback — opened from Profil ("Feedback och support"). */
export default function FeedbackRoute() {
  return <FeedbackFormScreen type="Feedback" />;
}
